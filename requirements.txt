# Patient Record Organizer - 1期 MVP 依赖

# PDF 解析
PyMuPDF>=1.23.0          # PDF 类型检测（文字型 vs 扫描件）

# Word 文档解析
python-docx>=1.0.0       # .docx 文件解析（读 + 写）
openpyxl>=3.1.0          # .xlsx 文件生成

# 模板引擎
Jinja2>=3.1.0             # 报告模板渲染

# Markdown 转换
markdown>=3.5.0           # Markdown → HTML

# HTTP 调用（OCR / ASR / LLM）
requests>=2.31.0          # 通用 HTTP 客户端
openai>=1.0.0             # SiliconFlow OpenAI 兼容接口
python-dotenv>=1.0.0      # 自动加载 .env 配置

# 测试
pytest>=7.4.0             # 测试框架

# 可选：PDF 输出（V2 启用）
# weasyprint>=60.0       # Markdown → PDF

# ── 可选依赖见 requirements-ocr.txt（离线 OCR 兜底）──
# 系统级二进制（tesseract + chi_sim）无法 pip 安装，详见 README「依赖」一节
